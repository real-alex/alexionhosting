# Hoist nothing by default; let workspace packages resolve cleanly.
# auto-install-peers keeps Next.js / NestJS peer deps satisfied.
auto-install-peers=true
strict-peer-dependencies=false
# Keep the lockfile deterministic across machines.
save-exact=false
